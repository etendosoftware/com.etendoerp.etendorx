das:
  url: http://das:8092
  grpc:
    ip: localhost
    port: 9090

classic:
  url: http://tomcat:8080

management:
  endpoints:
    web:
      exposure:
        include: # This enable all actuator endpoints. For production usage open only needed config
          '*'
spring:
  output:
    ansi:
      enabled: # Shows console output with colors
        ALWAYS
